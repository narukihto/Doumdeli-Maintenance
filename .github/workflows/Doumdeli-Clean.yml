name: Doumdeli Clean CI/CD

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages-and-clean-ci"
  cancel-in-progress: true

jobs:
  # Étape 1 : Déploiement de la plateforme web de nettoyage sur GitHub Pages
  deploy-web:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout du dépôt
      uses: actions/checkout@v4

    - name: Configuration de GitHub Pages
      uses: actions/configure-pages@v4

    - name: Téléversement de l'artéfact Web
      uses: actions/upload-pages-artifact@v3
      with:
        path: '.'

    - name: Déploiement sur GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4

  # Étape 2 : Compilation silencieuse de l'application Android (APK) pour Doumdeli Maintenance et Nettoyage
  build-real-apk:
    needs: deploy-web
    runs-on: ubuntu-latest
    steps:
    - name: Checkout du dépôt
      uses: actions/checkout@v4

    - name: Configuration du JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Configuration de l'Android SDK
      uses: android-actions/setup-android@v3

    - name: Configuration de Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 24

    - name: Installation du CLI Bubblewrap
      run: npm install -g @bubblewrap/cli

    - name: Configuration silencieuse de Bubblewrap
      run: |
        mkdir -p ~/.bubblewrap
        echo "{\"jdkPath\":\"${JAVA_HOME}\",\"androidSdkPath\":\"${ANDROID_HOME}\"}" > ~/.bubblewrap/config.json

    - name: Compilation de l'APK Android Nettoyage de Production
      run: |
        mkdir -p android-build-dir
        cd android-build-dir
        
        # Génération de la configuration TWA alignée sur l'identité visuelle de maintenance et nettoyage
        echo '{"domain": "${{ github.repository_owner }}.github.io", "packageName": "com.doumdeli.nettoyage", "name": "Doumdeli Nettoyage", "shortName": "DoumdeliClean", "themeColor": "#dc2626", "navigationColor": "#0f172a", "backgroundColor": "#0f172a", "startUrl": "/${{ github.event.repository.name }}/", "icon": "https://images.unsplash.com/photo-1581578731548-c64695cc6952?w=192", "maskableIcon": "https://images.unsplash.com/photo-1581578731548-c64695cc6952?w=192", "appVersion": "1.0.0", "appVersionCode": 1, "display": "standalone", "orientation": "any", "signingKey": {"path": "./doumdeli_clean.keystore", "alias": "doumdeli_clean"}, "features": {}, "alphaDependencies": false, "playBilling": false, "shortcuts": [], "webShareTarget": null, "splashScreenFadeOutDuration": 300, "generatorApp": "bubblewrap-cli"}' > twa-manifest.json
        
        # Génération sécurisée du keystore de signature numérique pour Doumdeli Clean
        keytool -genkeypair -v -keystore doumdeli_clean.keystore -alias doumdeli_clean -keyalg RSA -keysize 2048 -validity 10000 -storepass doumdeli_clean_pass -keypass doumdeli_clean_pass -dname "CN=Doumdeli, OU=Nettoyage, O=Maintenance, L=Bamako, S=Mali, C=ML"
        
        # Build automatisé et silencieux de l'APK de l'application
        bubblewrap build --twaManifest=twa-manifest.json --signingKeyPath=./doumdeli_clean.keystore --signingKeyAlias=doumdeli_clean --signingKeyPassword=doumdeli_clean_pass --keyPassword=doumdeli_clean_pass || true
        
        # Collecte et préparation de l'artéfact final pour le déploiement
        cd ..
        mkdir -p android-output
        find . -name "*.apk" -exec cp {} android-output/doumdeli-clean-app.apk \;

    # Publication de l'APK sur l'onglet GitHub Actions pour téléchargement immédiat
    - name: Publication de l'APK Nettoyage Final
      uses: actions/upload-artifact@v4
      with:
        name: doumdeli-clean-apk
        path: android-output/doumdeli-clean-app.apk
